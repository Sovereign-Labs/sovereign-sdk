{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RollupConfig",
  "description": "Rollup Configuration",
  "type": "object",
  "properties": {
    "da": {
      "description": "Data Availability service configuration.",
      "$ref": "#/$defs/MockDaConfig"
    },
    "monitoring": {
      "description": "Monitoring configuration.",
      "$ref": "#/$defs/MonitoringConfig"
    },
    "proof_manager": {
      "description": "Proof manager configuration. Required for `zk` and `optimistic` rollups,\noptional for `operator` rollups.",
      "anyOf": [
        {
          "$ref": "#/$defs/ProofManagerConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "runner": {
      "description": "Runner own configuration.",
      "$ref": "#/$defs/RunnerConfig"
    },
    "sequencer": {
      "description": "Sequencer (and batch builder) configuration.",
      "$ref": "#/$defs/SequencerConfig"
    },
    "storage": {
      "description": "Currently rollup config runner only supports storage path parameter",
      "$ref": "#/$defs/RollupDbConfig"
    }
  },
  "additionalProperties": false,
  "required": [
    "storage",
    "runner",
    "da",
    "sequencer",
    "monitoring"
  ],
  "$defs": {
    "Address": {
      "description": "Address",
      "type": "string",
      "pattern": "^sov1[a-zA-Z0-9]+$"
    },
    "BlockProducingConfig": {
      "description": "Configuration for block producing.",
      "oneOf": [
        {
          "description": "Blocks are produced at fixed time intervals, regardless of whether\nthere are transactions. This means empty blocks may be created.",
          "type": "object",
          "properties": {
            "periodic": {
              "type": "object",
              "properties": {
                "block_time_ms": {
                  "description": "The interval, in milliseconds, at which new blocks are produced.",
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                }
              },
              "required": [
                "block_time_ms"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "periodic"
          ]
        },
        {
          "description": "A new block is produced only when a batch blob (but not a proof blob) is submitted.\nEach block contains exactly one batch blob and zero or more proof blobs.",
          "type": "object",
          "properties": {
            "on_batch_submit": {
              "type": "object",
              "properties": {
                "block_wait_timeout_ms": {
                  "description": "The maximum time [`sov_rollup_interface::node::da::DaService::get_block_at`] will wait for a block to become available.\nIf this timeout elapses, an error is returned.\nIf set to `None`, [`DEFAULT_BLOCK_WAITING_TIME_MS`] is used.",
                  "type": [
                    "integer",
                    "null"
                  ],
                  "format": "uint64",
                  "minimum": 0
                }
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "on_batch_submit"
          ]
        },
        {
          "description": "A new block is produced when either a batch blob or a proof blob is submitted.\nEach block contains exactly one blob.",
          "type": "object",
          "properties": {
            "on_any_submit": {
              "type": "object",
              "properties": {
                "block_wait_timeout_ms": {
                  "description": "The maximum time [`sov_rollup_interface::node::da::DaService::get_block_at`] will wait for a block to become available.\nIf this timeout elapses, an error is returned.\nIf set to `None`, [`DEFAULT_BLOCK_WAITING_TIME_MS`] is used.",
                  "type": [
                    "integer",
                    "null"
                  ],
                  "format": "uint64",
                  "minimum": 0
                }
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "on_any_submit"
          ]
        },
        {
          "description": "Blocks are created manually, with no automatic production.",
          "type": "string",
          "const": "manual"
        }
      ]
    },
    "ConfiguredNodeRole": {
      "oneOf": [
        {
          "description": "This node runs as the leader. The leader is responsible for producing new batches.",
          "type": "string",
          "const": "Leader"
        },
        {
          "description": "This node runs as a replica, syncing its state from the leader.\nReplicas do not publish blobs to the DA layer, do not accept transactions via the API,\nand do not issue soft confirmations.",
          "type": "string",
          "const": "Replica"
        },
        {
          "description": "This node runs in replica mode with Postgres-based synchronization from the leader disabled.\nIt receives transactions exclusively through the DA layer.\nUse this mode when you want replica behavior without accepting transactions from the Leader only from the DA.",
          "type": "string",
          "const": "ReplicaNoLeaderSync"
        },
        {
          "description": "The node initially starts as a `Replica` and attempts to register itself as the `Leader`\nby sending a request to the Db to acquire leadership.\nIf successful, it becomes the `Leader` and all other nodes remain Replicas.\nIf the Leader goes down and fails to refresh its entry in the `Leader` table,\none of the `Replicas` will take over and assume the Leader role.",
          "type": "string",
          "const": "DbElected"
        }
      ]
    },
    "CorsConfiguration": {
      "description": "See [`HttpServerConfig::cors`].\n\n# Default\n\nCross-origin resource sharing (CORS) makes local development easier, so it's enabled by default\n\nIn production, one may want to disable it and let your reverse proxy\nhandle CORS instead.\nSecurity note:\nAllowing CORS in production allows making requests to rollup node any website on the Internet.",
      "oneOf": [
        {
          "description": "Enables CORS with a permissive policy, intended for local development.",
          "type": "string",
          "const": "permissive"
        },
        {
          "description": "Disables CORS.",
          "type": "string",
          "const": "restrictive"
        }
      ]
    },
    "FailureBehavior": {
      "description": "Configurable failure behavior for testing error handling in consumers of MockDa.\nThis allows tests to inject failures at specific points during DA operations.",
      "oneOf": [
        {
          "description": "No failures (default behavior).",
          "type": "string",
          "const": "none"
        },
        {
          "description": "Fail `get_block_at` or `get_block_header_at` after N successful calls.\nThe counter decrements on each call; when it reaches 0, failures may occur\nbased on the configured probability.",
          "type": "object",
          "properties": {
            "fail_after_n_calls": {
              "type": "object",
              "properties": {
                "failure_probability": {
                  "description": "Probability of failure (0-100). 100 = always fail, 0 = never fail.",
                  "type": "integer",
                  "format": "uint8",
                  "default": 100,
                  "maximum": 255,
                  "minimum": 0
                },
                "remaining": {
                  "description": "Number of successful calls remaining before failures may start.",
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                }
              },
              "required": [
                "remaining"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "fail_after_n_calls"
          ]
        },
        {
          "description": "Trigger a reorg (shuffle non-finalized blobs) when `get_block_at` or\n`get_block_header_at` is called for a specific height.\nAfter the reorg is triggered, subsequent calls proceed normally.",
          "type": "object",
          "properties": {
            "reorg_during_call": {
              "type": "object",
              "properties": {
                "trigger_at_height": {
                  "description": "The height that triggers the reorg.",
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                }
              },
              "required": [
                "trigger_at_height"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "reorg_during_call"
          ]
        },
        {
          "description": "Add artificial delays to `get_block_at` or `get_block_header_at` after N calls.\nUseful for testing timeout handling and slow DA scenarios.",
          "type": "object",
          "properties": {
            "delay_after_n_calls": {
              "type": "object",
              "properties": {
                "delay_range_ms": {
                  "description": "Range of delay in milliseconds. A random value from this range is used.",
                  "$ref": "#/$defs/Range_of_uint64"
                },
                "remaining": {
                  "description": "Number of calls before delays start.",
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0
                }
              },
              "required": [
                "remaining",
                "delay_range_ms"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "delay_after_n_calls"
          ]
        }
      ]
    },
    "HexHash": {
      "description": "32 bytes in hexadecimal format, with `0x` prefix.",
      "type": "string",
      "pattern": "^0x[a-fA-F0-9]{64}$"
    },
    "HttpServerConfig": {
      "description": "Configuration for HTTP server(s) exposed by the node.",
      "type": "object",
      "properties": {
        "bind_host": {
          "description": "Server host.",
          "type": "string"
        },
        "bind_port": {
          "description": "Server port.",
          "type": "integer",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "cors": {
          "description": "Enable or disable CORS policy headers. Enabled by default.",
          "$ref": "#/$defs/CorsConfiguration",
          "default": "permissive"
        },
        "public_address": {
          "description": "The fully qualified public name of the server, in case the rollup node is running behind a proxy.\nFor instance:\n```toml\npublic_address = \"https://rollup.example.com\"\n```",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "bind_host",
        "bind_port"
      ]
    },
    "IpAddrOrNet": {
      "description": "Schema-only mirror of the wire form accepted by [`deserialize_ip_custom_limits`]: a bare IP\naddress or a CIDR network, IPv4 or IPv6. Exists solely to give\n[`SovRateLimiterConfig::ip_custom_limits`] an accurate `anyOf` JSON Schema instead of a bare\n`string`. Never constructed.",
      "anyOf": [
        {
          "type": "string",
          "format": "ipv4"
        },
        {
          "$ref": "#/$defs/Ipv4Net"
        },
        {
          "type": "string",
          "format": "ipv6"
        },
        {
          "$ref": "#/$defs/Ipv6Net"
        }
      ]
    },
    "Ipv4Net": {
      "title": "IPv4 network",
      "description": "An IPv4 address with prefix length",
      "type": "string",
      "examples": [
        "0.0.0.0/0",
        "192.168.0.0/24"
      ],
      "maxLength": 18,
      "pattern": "^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\\/(?:3[0-2]|[1-2][0-9]|[0-9])$"
    },
    "Ipv6Net": {
      "title": "IPv6 network",
      "description": "An IPv6 address with prefix length",
      "type": "string",
      "examples": [
        "::/0",
        "fd00::/32"
      ],
      "maxLength": 43,
      "pattern": "^[0-9A-Fa-f:\\.]+\\/(?:[0-9]|[1-9][0-9]|1[0-1][0-9]|12[0-8])$"
    },
    "LeaderElectionConfig": {
      "description": "Configuration for leader election timing.",
      "type": "object",
      "properties": {
        "grace_period_millis": {
          "description": "Minimum time in milliseconds after leader acquisition before another node can take over.\nPrevents rapid leader flapping.",
          "type": "integer",
          "format": "uint64",
          "default": 10000,
          "minimum": 0
        },
        "heartbeat_interval_millis": {
          "description": "Interval in milliseconds between heartbeat updates.",
          "type": "integer",
          "format": "uint64",
          "default": 100,
          "minimum": 0
        },
        "leader_timeout_millis": {
          "description": "Maximum time in milliseconds without a heartbeat before a leader is considered inactive.",
          "type": "integer",
          "format": "uint64",
          "default": 500,
          "minimum": 0
        }
      }
    },
    "Limits": {
      "type": "object",
      "properties": {
        "refill_rate": {
          "description": "The refill rate of buckets. E.g. if refill_rate = 5, the user's rate limiting bucket will be refilled up to five times every batch.\nValues between 1 and 20 are recommended starting points.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "resources_per_bucket": {
          "description": "Determines the threshold for rate-limiting requests.\nThe resources allocated per user per rate-limiting bucket, defined in units of 1/1000th of a full batch. E.g. resources_per_bucket = 10 would mean each bucket allows the user to use 1% of a full batch capacity.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "resources_per_bucket",
        "refill_rate"
      ]
    },
    "MockAddress": {
      "description": "Mock address; 32 bytes in hex-encoded format",
      "type": "string",
      "pattern": "^[a-fA-F0-9]{64}$"
    },
    "MockDaConfig": {
      "description": "The configuration for Mock Da.",
      "type": "object",
      "properties": {
        "block_producing": {
          "description": "How MockDaService should produce blocks.",
          "$ref": "#/$defs/BlockProducingConfig",
          "default": {
            "on_batch_submit": {
              "block_wait_timeout_ms": 120000
            }
          }
        },
        "connection_string": {
          "description": "Connection string to the database for storing Da Data.\n  - \"sqlite://demo_data/da.sqlite?mode=rwc\"\n  - \"sqlite::memory:\"\n  - \"postgresql://root:hunter2@aws.amazon.com/mock-da\"",
          "type": "string"
        },
        "failure_behavior": {
          "description": "Configures failure injection for testing.\nDefaults to `FailureBehavior::None` (no failures).",
          "$ref": "#/$defs/FailureBehavior",
          "default": "none"
        },
        "finalization_blocks": {
          "description": "Defines how many blocks progress to finalization.",
          "type": "integer",
          "format": "uint32",
          "default": 0,
          "minimum": 0
        },
        "randomization": {
          "description": "If specified, [`StorableMockDaLayer`] will add randomization to non-finalized blocks.",
          "anyOf": [
            {
              "$ref": "#/$defs/RandomizationConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "sender_address": {
          "description": "The address to use to \"submit\" blobs on the mock da layer.",
          "$ref": "#/$defs/MockAddress"
        }
      },
      "additionalProperties": false,
      "required": [
        "connection_string",
        "sender_address"
      ]
    },
    "MonitoringConfig": {
      "description": "Configuration of Sovereign monitoring",
      "type": "object",
      "properties": {
        "max_datagram_size": {
          "description": "Defines how many measurements a rollup node will accumulate before sending it to the Telegraf.\nIt is expected from the rollup node to produce metrics all the time,\nso measurements are buffered by size and not sent by time.\nand below 67 KB, which is the maximal UDP packet size.\nIt also means that if a single serialized metric is larger than this value, a UDP packet will be larger.\n\nNote: to disable buffering, set this value to `Some(1)`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "max_pending_metrics": {
          "description": "How many metrics are allowed to be in pending state, before new metrics will be dropped.\nThis is a number of metrics, not serialized bytes.\nThe total number of bytes to be held in memory might vary per metric + `max_datagram_size`",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "telegraf_address": {
          "description": "A UDP or TCP socket where Telegraf has `inputs.socket_listener`, something like `udp://127.0.0.1:8094`.\nLocalhost is preferred for latency and reliability reasons.",
          "$ref": "#/$defs/TelegrafSocketConfig"
        },
        "tokio_runtime_metrics_interval_millis": {
          "description": "How often to collect tokio runtime metrics. Defaults to 500ms.",
          "type": "integer",
          "format": "uint64",
          "default": 500,
          "minimum": 0
        }
      },
      "required": [
        "telegraf_address"
      ]
    },
    "PostgresConfig": {
      "description": "Postgres DB config.",
      "type": "object",
      "properties": {
        "leader_election": {
          "description": "Configuration for leader election timing.",
          "$ref": "#/$defs/LeaderElectionConfig",
          "default": {
            "grace_period_millis": 10000,
            "heartbeat_interval_millis": 100,
            "leader_timeout_millis": 500
          }
        },
        "node_id": {
          "description": "Id of the node.",
          "type": "string"
        },
        "node_role": {
          "$ref": "#/$defs/ConfiguredNodeRole"
        },
        "postgres_connection_string": {
          "description": "Connection string.",
          "type": "string"
        }
      },
      "required": [
        "postgres_connection_string",
        "node_id",
        "node_role"
      ]
    },
    "PreferredSequencerConfig": {
      "description": "Configuration for [`PreferredSequencer`].",
      "type": "object",
      "properties": {
        "batch_execution_time_limit_millis": {
          "description": "Target time in milliseconds to spend executing all the txs in a single batch. Batches will be closed when they exceed this value.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "db_event_channel_size": {
          "description": "The number of events that can be buffered in the database event channel while `update_state` is running.\nThis value needs to be increased at higher TPS to avoid blocking the sequencer.",
          "type": "integer",
          "format": "uint",
          "default": 10000,
          "minimum": 0
        },
        "disable_state_root_consistency_checks": {
          "description": "When enabled, the sequencer will skip some expensive consistency checks\non the state root. This means that bugs in the implementation are less likely to be detected\nbut may improve performance and allows the sequencer to continue operating in case of known bugs.",
          "type": "boolean",
          "default": false
        },
        "events_channel_size": {
          "description": "The size of the Tokio channel used to stream events.\n\nDon't deviate from the default unless you know what you're doing.",
          "type": "integer",
          "format": "uint",
          "default": 10000,
          "minimum": 0
        },
        "future_nonce_transaction_timeout_millis": {
          "description": "The timeout after which a transaction with a near-future nonce (bounded by\n`maximum_future_nonce_delta`) will be dropped and forgotten by the sequencer. Increasing\nthis value increases the leniency with which out-of-order transactions are considered a\n\"simultaneous batch\" which should be reordered, but can increase memory usage and queue\nlock contention.",
          "type": "integer",
          "format": "uint64",
          "default": 2000,
          "minimum": 0
        },
        "ideal_lag_behind_finalized_slot": {
          "description": "The ideal lag behind the finalized slot number.",
          "type": "integer",
          "format": "uint64",
          "default": 10,
          "minimum": 0
        },
        "maximum_future_nonce_delta": {
          "description": "The farthest nonce into the future that the sequencer will accept and queue. This directly\nimpacts the maximum \"batch\" of transactions that can be simultaneously sent to the\nsequencer out of order.",
          "type": "integer",
          "format": "uint64",
          "default": 100,
          "minimum": 0
        },
        "minimum_profit_per_tx": {
          "description": "The minimum fee that the preferred sequencer is willing to accept, denominated in rollup tokens. Defaults to zero.\nSequencers should set this to a non-zero value if they wish to cover their DA costs.",
          "type": "integer",
          "format": "uint128",
          "default": 0,
          "minimum": 0
        },
        "num_cache_warmup_workers": {
          "description": "The number of workers that warm up the main executor cache.",
          "type": "integer",
          "format": "uint",
          "default": 3,
          "minimum": 0
        },
        "postgres_config": {
          "description": "Optional. When present, Postgres will be used as a database instead of\nRocksDB.",
          "anyOf": [
            {
              "$ref": "#/$defs/PostgresConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "rate_limiter": {
          "description": "Configuration for rate-limiting the sequencer.",
          "anyOf": [
            {
              "$ref": "#/$defs/SovRateLimiterConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "recovery_strategy": {
          "description": "Strategy for handling recovery scenarios in the preferred sequencer.",
          "$ref": "#/$defs/RecoveryStrategy"
        }
      },
      "additionalProperties": false,
      "required": [
        "recovery_strategy",
        "batch_execution_time_limit_millis"
      ]
    },
    "ProofManagerConfig": {
      "description": "Prover service configuration.",
      "type": "object",
      "properties": {
        "aggregated_proof_block_jump": {
          "description": "The \"distance\" measured in the number of blocks between two consecutive aggregated proofs.",
          "type": "integer",
          "format": "uint",
          "minimum": 1
        },
        "eager_proof_submission": {
          "description": "When true, proofs are submitted as each block arrives.\nWhen false, submission is deferred until the batch is full, then submitted concurrently.",
          "type": "boolean",
          "default": true
        },
        "max_concurrent_proof_blobs": {
          "description": "Maximum number of proof blobs sent in parallel. Batch blobs are not\ncounted against this limit.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "max_number_of_aggregated_proofs_in_memory": {
          "description": "Maximum number of completed aggregation windows that can be buffered in\nmemory between the intake task and the aggregator task. When the\naggregator falls behind by this many windows, intake stalls and\nback-pressure propagates upstream.",
          "type": "integer",
          "format": "uint",
          "minimum": 1
        },
        "max_number_of_transitions_in_db": {
          "description": "A number of state transition info entries are allowed to be stored in the database.\nWhen the number is exceeded, older entries are removed.",
          "type": "integer",
          "format": "uint64",
          "minimum": 1
        },
        "max_number_of_transitions_in_memory": {
          "description": "A number of state transition info entries are allowed to be kept in memory.\nIf the number is exceeded, rollup execution will be blocked until provers cathes up.",
          "type": "integer",
          "format": "uint64",
          "minimum": 1
        },
        "prover_address": {
          "description": "The prover receives rewards to this address.",
          "$ref": "#/$defs/Address"
        },
        "prover_thread_count_override": {
          "description": "Override for the number of prover threads. When `None`, defaults to\n`2 * aggregated_proof_block_jump + 1`, which covers inner proofs for\nthe current and next batch plus one outer-aggregation worker.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "default": null,
          "minimum": 1
        }
      },
      "required": [
        "aggregated_proof_block_jump",
        "prover_address",
        "max_number_of_transitions_in_db",
        "max_number_of_transitions_in_memory",
        "max_number_of_aggregated_proofs_in_memory",
        "max_concurrent_proof_blobs"
      ]
    },
    "RandomizationBehaviour": {
      "description": "Defines the behavior of randomization applied to blobs or blocks.\n\nThis configurable behavior determines how blobs are processed and returned to the caller\nduring various stages of the block production process.\nRandomization may involve reordering, shuffling, skipping, or altering the chain's length,\nwhile preserving certain constraints such as finality.",
      "oneOf": [
        {
          "description": "Blobs inside a single block are rearranged in a random order when read.\nThis does not affect the boundary between blocks, meaning no blob will\ncross its original block's boundary.\n\nNotes:\n- Does not impact how new blocks are produced, and block hashes are not changed.\n- Finalized blocks may have their blobs reordered within this mode.\n- This does not change the stored order of blobs.\n- If randomization is disabled, blobs will be returned in their original order.\n- Order guaranteed to be deterministic for each block with the same randomizer configuration.",
          "type": "string",
          "const": "out_of_order_blobs"
        },
        {
          "description": "Rewinds the chain to a specific height, chosen randomly between\nthe most recently finalized block and the current head of the chain.\n\nThis operation adjusts the chain height but maintains finalization constraints.",
          "type": "string",
          "const": "rewind"
        },
        {
          "description": "Makes `get_head_block_header` and `get_last_finalized_block_header` randomly\nreturn block headers below the actual finalized height.\n\nThis simulates scenarios where the DA layer reports stale data,\nuseful for testing rollup resilience to DA layer inconsistencies.\n\nBehavior:\n- Each call advances the internal RNG, returning a different height each time.\n- `get_last_finalized_block_header()` stores its result as a floor for head.\n- `get_head_block_header()` returns `max(computed_height, last_finalized_floor)`.\n- To guarantee `head >= finalized`, call `get_last_finalized_block_header()` first.\n\nNotes:\n- Does not affect actual block production or chain state.\n- Triggered probabilistically based on `reorg_interval` configuration.\n- Heights are deterministic given the same seed and call sequence.",
          "type": "object",
          "properties": {
            "rewind_below_last_finalized": {
              "type": "object",
              "properties": {
                "max_depth": {
                  "description": "Maximum number of blocks below finalized height to report.\nRandom height is chosen between `max(0, finalized - max_depth)` and `finalized`.",
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0
                }
              },
              "required": [
                "max_depth"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "rewind_below_last_finalized"
          ]
        },
        {
          "description": "Combines blob shuffling with chain height adjustment:\n\n1. All non-finalized blobs, including those being added to a new block,\n   are shuffled across all blobs that are part of the new chain state.\n2. The chain height is adjusted (rewound or extended) within the constraints\n   of the finality window.\n\n**Constraints**:\n- Rewinding can only occur as far back as the finality window allows.\n- Extending is not possible if the finality window is already full.\n- Rewinding is not triggered if there is only one non-finalized block.\n- The specified percentage of blobs (`drop_percent`) is always respected.",
          "type": "object",
          "properties": {
            "shuffle_and_resize": {
              "type": "object",
              "properties": {
                "adjust_head_height": {
                  "description": "Range of possible adjustments to the chain head height:\n- Negative values represent rewinding the chain length (moving backward in height).\n- Positive values represent extending the chain length (adding new blocks).\n- This adjustment is constrained by the finality window.\n\nThe actual value is selected by [`crate::storable::layer::Randomizer`] from this range.",
                  "$ref": "#/$defs/Range_of_int32"
                },
                "drop_percent": {
                  "description": "Percentage of blobs to be permanently skipped during this process.\n\nA value of `100` means all non-finalized blobs will be dropped.",
                  "type": "integer",
                  "format": "uint8",
                  "maximum": 255,
                  "minimum": 0
                }
              },
              "required": [
                "drop_percent",
                "adjust_head_height"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "shuffle_and_resize"
          ]
        }
      ]
    },
    "RandomizationConfig": {
      "description": "Configuration for randomization applied.\n\nThis struct defines how randomization is performed, including the seed for the randomizer,\nthe timing of chain reorganization, and the specific randomization behavior applied.",
      "type": "object",
      "properties": {
        "behaviour": {
          "description": "Defines the specific behavior of the randomizer during randomization.\n\nThis determines how blobs or blocks are processed, including their ordering,\nshuffling, skipping, or potential adjustments affecting the chain.",
          "$ref": "#/$defs/RandomizationBehaviour"
        },
        "reorg_interval": {
          "description": "The interval, in produced blocks, at which chain reorganization may occur.\nApplicable for all cases except [`RandomizationBehaviour::OutOfOrderBlobs`],\nwhich does not affect block production.\n\nFor a range `m..n`:\n- A reorganization can occur at every `m`-th block produced after the last reorganization.\n- A reorganization will definitely occur at or before the `n`-th block produced since the last reorganization.\n\nNote:\n- The interval is counted starting from the height at which the last reorganization happened,\n  rather than the current state of the chain.\n- This allows the chain to progress consistently within the specified bounds between reorganizations.",
          "$ref": "#/$defs/Range_of_uint32"
        },
        "seed": {
          "description": "Seed used by the randomizer to ensure deterministic but randomized behavior.",
          "$ref": "#/$defs/HexHash"
        }
      },
      "required": [
        "seed",
        "reorg_interval",
        "behaviour"
      ]
    },
    "Range_of_int32": {
      "type": "object",
      "properties": {
        "end": {
          "type": "integer",
          "format": "int32"
        },
        "start": {
          "type": "integer",
          "format": "int32"
        }
      },
      "required": [
        "start",
        "end"
      ]
    },
    "Range_of_uint32": {
      "type": "object",
      "properties": {
        "end": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "start": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "start",
        "end"
      ]
    },
    "Range_of_uint64": {
      "type": "object",
      "properties": {
        "end": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "start": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "start",
        "end"
      ]
    },
    "RecoveryStrategy": {
      "description": "Strategy for handling the scenario where the preferred sequencer finds itself close to or past\ndeferred_slots_count in the past, i.e. risking its soft confirmations being invalidated due to\nthe possibility of a non-preferred (deferred) batch having been included.",
      "oneOf": [
        {
          "description": "Do not attempt recovery, shutdown the sequencer instead. The user may attempt to resume\noperation either by swapping to TryToSave, or deleting everything from the preferred\nsequencer database (cancelling ALL pending soft confirmations!).",
          "type": "string",
          "const": "None"
        },
        {
          "description": "Attempt to recover by flushing batches and catching up with the chain. Triggers a bit more\nconservatively to attempt to preserve soft confirmations (but if the sequencer was offline,\nthis will likely make no difference). If some soft confirmations have indeed been\ninvalidated, the sequencer will be penalized for every invalid batch!",
          "type": "string",
          "const": "TryToSave"
        }
      ]
    },
    "RollupDbConfig": {
      "description": "Configuration for Sovereign Rollup node database.",
      "type": "object",
      "properties": {
        "kernel_commit_concurrency": {
          "description": "Number of concurrent commit workers for the kernel state.\nMore details at [`Options::commit_concurrency`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "kernel_hashtable_buckets": {
          "description": "Cannot be changed for the existing database.\nMore details at [`Options::hashtable_buckets`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "kernel_leaf_cache_size": {
          "description": "Leaf cache size for kernel state.\nMore details at [`Options::leaf_cache_size`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "kernel_page_cache_size": {
          "description": "Page cache size for kernel state.\nMore details at [`Options::page_cache_size`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "kernel_page_cache_upper_levels": {
          "description": "Page cache upper levels for kernel state.\nMore details at [`Options::page_cache_upper_levels`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "kernel_preallocate_ht": {
          "description": "Sets whether to preallocate the hashtable file for kernel db.\nMore details at [`Options::preallocate_ht`]",
          "type": [
            "boolean",
            "null"
          ]
        },
        "ledger_db_path": {
          "description": "Optional custom path for the ledger database.\n\nWhen set, ledger DB data is stored at this path. When not set, the ledger DB is stored\nunder [`RollupDbConfig::path`] (the existing behavior).",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "Path where all databases are stored",
          "type": "string"
        },
        "pruner_block_interval": {
          "description": "Pruner\nDefines how often pruner is going to be started.\nMeasure by DA blocks.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "pruner_max_batch_size": {
          "description": "Maximum number of keys to prune in a single batch.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "pruner_versions_to_keep": {
          "description": "These many versions will be available for historical querying.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "state_cache_size": {
          "description": "The size of the cache which holds hot key-value pairs in the flat state database. Default is 1 GiB.\nA larger cache size can improve execution speed at the cost of more memory usage.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "user_commit_concurrency": {
          "description": "Number of concurrent commit workers for the user state.\nMore details at [`Options::commit_concurrency`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "user_hashtable_buckets": {
          "description": "Value is determined by the expected size of the state. Recommended to start with 15_000_000.\nCannot be changed for the existing database.\nMore details at [`Options::hashtable_buckets`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "user_leaf_cache_size": {
          "description": "Leaf cache size for user state.\nMore details at [`Options::leaf_cache_size`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "user_page_cache_size": {
          "description": "Page cache size for user state.\nMore details at [`Options::page_cache_size`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "user_page_cache_upper_levels": {
          "description": "Page cache upper levels for user state.\nMore details at [`Options::page_cache_upper_levels`]",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "user_preallocate_ht": {
          "description": "Sets whether to preallocate the hashtable file for user db.\nMore details at [`Options::preallocate_ht`]",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "required": [
        "path"
      ]
    },
    "RollupHeight": {
      "description": "A rollup \"block number\". Rollup heights increase in order (1, 2, 3, ...),\nregardless of what happens on the underlying DA layer.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "RunnerConfig": {
      "description": "Configuration for StateTransitionRunner.",
      "type": "object",
      "properties": {
        "concurrent_sync_tasks": {
          "description": "How many concurrent tasks to prefetch DA block during sync.",
          "type": "integer",
          "format": "uint8",
          "default": 5,
          "maximum": 255,
          "minimum": 0
        },
        "da_polling_interval_ms": {
          "description": "Polling interval for the DA service to check the sync status (in milliseconds).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "http_config": {
          "description": "HTTP Server configuration: On this socket REST API and RPC endpoints are going to listen.",
          "$ref": "#/$defs/HttpServerConfig"
        },
        "pre_fetched_blocks_capacity": {
          "description": "How many blocks maximum will be stored in memory.\nIt affects total concurrent requests done do DA service.\nIt should be larger than `concurrent_sync_tasks`.",
          "type": "integer",
          "format": "uint",
          "default": 20,
          "minimum": 1
        },
        "save_tx_bodies": {
          "description": "Whether to save transaction bodies to the database.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "da_polling_interval_ms",
        "http_config"
      ]
    },
    "SeqConfigExtension": {
      "description": "Configuration data used by sequencer extensions, such as EVM endpoints.",
      "type": "object",
      "properties": {
        "max_log_limit": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "response_size_limit": {
          "description": "The maximum size of the response to the eth_getLogs RPC endpoint. Use 1 MiB - 30 KiB for a safe default.",
          "type": "integer",
          "format": "uint",
          "default": 1017856,
          "minimum": 0
        }
      },
      "required": [
        "max_log_limit"
      ]
    },
    "SequencerConfig": {
      "description": "Sequencer configuration.",
      "type": "object",
      "properties": {
        "admin_addresses": {
          "description": "The list of addresses that are allowed to perform admin operations on\nthe sequencer.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Address"
          }
        },
        "automatic_batch_production": {
          "description": "When enabled, submitted transactions are periodically assembled into\nbatches and automatically posted to the DA layer. When disabled, the\nbatch production endpoint has to be called explicitly.",
          "type": "boolean",
          "default": true
        },
        "blob_processing_timeout_secs": {
          "description": "Maximum time in seconds to wait for a blob to be processed, since it has been published to DA.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "dropped_tx_ttl_secs": {
          "description": "For how many seconds the sequencer keeps track of dropped transactions\nafter being done with them.\n\nLarger values result in higher memory usage, but better tx status\ntracking for users.",
          "type": "integer",
          "format": "uint64",
          "default": 60,
          "minimum": 0
        },
        "extension": {
          "description": "Extensions to the sequencer config (for example evm related configuration).",
          "anyOf": [
            {
              "$ref": "#/$defs/SeqConfigExtension"
            },
            {
              "type": "null"
            }
          ]
        },
        "max_allowed_node_distance_behind": {
          "description": "The sequencer won't process incoming requests unless the node is within\nthis many blocks or ahead of the sequencer.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "max_batch_size_bytes": {
          "description": "Maximum size of a batch.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "max_concurrent_batch_blobs": {
          "description": "Maximum number of batch blobs sent in parallel before throttling\nnew batch creation. Proof blobs are not counted against this limit.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "rollup_address": {
          "description": "Rollup address of the sequencer.",
          "$ref": "#/$defs/Address"
        }
      },
      "oneOf": [
        {
          "description": "A \"Standard\" sequencer, which can post transactions to the rollup but not give soft confirmations.",
          "type": "object",
          "properties": {
            "standard": {
              "$ref": "#/$defs/StdSequencerConfig"
            }
          },
          "required": [
            "standard"
          ]
        },
        {
          "description": "A \"Preferred\" sequencer which is allowed to give soft confirmations.",
          "type": "object",
          "properties": {
            "preferred": {
              "$ref": "#/$defs/PreferredSequencerConfig"
            }
          },
          "required": [
            "preferred"
          ]
        }
      ],
      "required": [
        "max_allowed_node_distance_behind",
        "rollup_address",
        "max_batch_size_bytes",
        "max_concurrent_batch_blobs",
        "blob_processing_timeout_secs"
      ],
      "unevaluatedProperties": false
    },
    "SovRateLimiterConfig": {
      "type": "object",
      "properties": {
        "address_custom_limits": {
          "type": "array",
          "items": {
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "prefixItems": [
              {
                "$ref": "#/$defs/Address"
              },
              {
                "$ref": "#/$defs/Limits"
              }
            ]
          }
        },
        "default_limits": {
          "description": "Default limits.",
          "$ref": "#/$defs/Limits"
        },
        "height_for_gas_limit_computation": {
          "description": "Rate limiting on gas is currently disabled, so this param has no impact on runtime behavior.\n\nThis height is used to statically compute the gas limit for the rate limiter. (If this value is less than or equal to CHANGE_GAS_LIMIT_AFTER_HEIGHT in constants.toml,\nthe gas limit will *always* be computed using the initial gas limit for rate limiting. If it is greater, the gas limit will be computed using the updated gas limit.)\nEven after rate limiting based on gas is enabled, you can safely change this param at any time since it only impacts off-chain code.",
          "$ref": "#/$defs/RollupHeight"
        },
        "ip_custom_limits": {
          "description": "Per-network rate limits. Accepts CIDR notation (`10.0.0.0/8`) or a bare\nIP address (`192.168.1.5`); a bare address is treated as a host network\n(`/32` for IPv4, `/128` for IPv6).",
          "type": "array",
          "items": {
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "prefixItems": [
              {
                "$ref": "#/$defs/IpAddrOrNet"
              },
              {
                "$ref": "#/$defs/Limits"
              }
            ]
          }
        },
        "max_nb_of_concurrent_users_in_rate_limiter": {
          "description": "The cache size for the rate limiter.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "max_requests_per_second": {
          "description": "The maximum number of requests allowed per second.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "max_nb_of_concurrent_users_in_rate_limiter",
        "max_requests_per_second",
        "default_limits",
        "address_custom_limits",
        "ip_custom_limits"
      ]
    },
    "StdSequencerConfig": {
      "description": "Configuration for [`StdSequencer`].",
      "type": "object",
      "properties": {
        "max_batch_size_bytes": {
          "description": "Maximum size of a batch. The sequencer will not build batches larger\nthan this size.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 1
        },
        "mempool_max_txs_count": {
          "description": "Maximum number of transactions in mempool. Once this limit is reached,\nthe batch builder will evict older transactions.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 1
        }
      },
      "additionalProperties": false
    },
    "TelegrafSocketConfig": {
      "description": "Config of receiving `inputs.socket_listener`\n<https://www.influxdata.com/blog/telegraf-socket-listener-input-plugin/>",
      "type": "object",
      "properties": {
        "addr": {
          "description": "Socket",
          "type": "string"
        },
        "transport": {
          "description": "Transport",
          "$ref": "#/$defs/Transport"
        }
      },
      "required": [
        "transport",
        "addr"
      ]
    },
    "Transport": {
      "description": "Variant of transport supported by metrics sender.",
      "type": "string",
      "enum": [
        "tcp",
        "udp"
      ]
    }
  }
}
