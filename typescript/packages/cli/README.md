# @sovereign-sdk/cli

CLI utilities for Sovereign SDK.

## Usage

```bash
npx @sovereign-sdk/cli upgrade add-chainhash-override \
  --constants-path ./constants.toml \
  --blocks-from-now 10000
```

You can also use `--end-height` instead of `--blocks-from-now`.
