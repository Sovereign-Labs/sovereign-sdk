{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "target/**"],
      "inputs": ["src/**", "package.json", "Cargo.toml"]
    },
    "test": {
      "dependsOn": ["^build", "build"],
      "outputs": [],
      "inputs": ["src/**", "tests/**", "package.json", "Cargo.toml"]
    }
  }
}
