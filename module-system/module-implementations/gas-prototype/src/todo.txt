2. Gas crate (sov-state depends on it)
3. Add new() method to ModuleInfo
4  Hash, Signature etc take WorkingSet
5. Add GasUnit to WorkingSet (Generic)
6. Todo add GasUnit to Context
7. Todo should we pass GasPrice in WorkingSet or Context
8. How do we pass GasPrice & GasUnit to BatchBuilder, Rpc macros etc
9. Should the Context have `charge_gas(gas_cost, working_set)`
10. DO we want multiple gas costs and gas prices
11. set, get, hash, etc can run out of gas so they return Result